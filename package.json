{
  "name": "courthub",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "smoke": "node scripts/smoke.mjs",
    "test": "npm run check && npm run smoke && node scripts/workspace-smoke.mjs && node scripts/play-designer-smoke.mjs",
    "dev": "vercel dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
